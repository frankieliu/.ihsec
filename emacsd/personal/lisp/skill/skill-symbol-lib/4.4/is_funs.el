(put 'ise_funs 'symbol-type "Interactive Simulation Environment")
(setq ise_funs '(
("iseStartInteractive" "" "Starts the default interactive simulation environment. The simulation environment must be initialized before iseStartInteractive is called; that is, the simInitEnv or simInitEnvWithArgs function must have been called.")

))
