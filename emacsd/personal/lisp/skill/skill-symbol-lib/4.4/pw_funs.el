(put 'pwr_funs 'symbol-type "Floorplanning")
(setq pwr_funs '(
("pwrPinPairRoute" "d_cv nil " "Creates and routes pin pairs for supply and ground. Global routing must be complete (after you run grClose).")

))
